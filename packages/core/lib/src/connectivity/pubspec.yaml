name: connectivity
description: Check if device can connect to the internet
version: 1.0.0
resolution: workspace

environment:
  sdk: ^3.10.4

dependencies:
  internet_connection_checker: ^3.0.1
  injectable:
  app_log:
    path: ../app_log

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
