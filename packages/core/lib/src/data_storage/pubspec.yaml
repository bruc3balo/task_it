name: data_storage
description: Defines how data can be stored in local storage.
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.4

# Add regular dependencies here.
dependencies:
  base:
    path: ../../../../base

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
