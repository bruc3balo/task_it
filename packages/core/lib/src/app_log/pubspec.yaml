name: app_log
description: Logging module of whole application
version: 1.0.0
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.4

dependencies:
  logger:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
