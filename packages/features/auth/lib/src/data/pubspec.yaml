name: auth_data
description: A starting point for Dart libraries or applications.
version: 1.0.0
resolution: workspace
publish_to: none

environment:
  sdk: ^3.10.7

dependencies:
  auth_domain:
    path: ../domain
  core:
    path: ../../../../../core
  injectable:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
