name: auth_presentation
description: "Auth presentation layer"
version: 0.0.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.10.7
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  auth_domain:
    path: ../domain
  core:
    path: ../../../../../core
  bloc:
  injectable: 

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
